int force = 0;
int temperature = 0
  
void setup()
{
  pinMode(A0, INPUT);
  pinMode(3, OUTPUT);
}

void loop()
{
  force = analogRead(A0);
  analogWrite(3, force);
  if (force > 0)
  {
    digitalWrite (3, HIGH);
  }
  else
  {
    digitalWrite (3, LOW);
  }
  
  delay(1000); 
}
